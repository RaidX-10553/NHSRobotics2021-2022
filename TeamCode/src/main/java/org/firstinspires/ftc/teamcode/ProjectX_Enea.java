/*
package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.HardwareMap;
import com.qualcomm.robotcore.util.ElapsedTime;

import org.firstinspires.ftc.robotcore.external.Telemetry;

@TeleOp

public class ProjectX_Enea {
    public ElapsedTime runtime = new ElapsedTime();
    public DcMotor Front = null;
    public DcMotor Left = null;
    public DcMotor Right = null;
    public DcMotor Back = null;
    public HardwareMap hardwareMap = null;

    @Override
    public void init(hardwareMap) {
        Telemetry.addData("Status", "Initialized");
        Front = hardwareMap.dcMotor.get("FL");
        Right = hardwareMap.dcMotor.get("FR");
        Left = hardwareMap.dcMotor.get("BL");
        Back = hardwareMap.dcMotor.get("BR");
        Right.setDirection(DcMotor.Direction.REVERSE);
        Back.setDirection(DcMotor.Direction.REVERSE);
    }
}
enea is a tard
 */